# Checklist assessment: Home roll-up (R12)

**Assessed**: 2026-09-06, against the R12 artifacts as they stand in this worktree
(`spec.md`, `plan.md`, `research.md`, `data-model.md`, `quickstart.md`, `contracts/queries.md`,
`contracts/screens.md`, `tasks.md`, `AGENTS.md`) and the shipped R1–R11 code under `src/`.

**No R12 implementation code exists.** `src/features/home/` is absent and
`src/app/(app)/home/page.tsx` still returns `null`. That fact turns out to block almost nothing:
all three checklists declare themselves *reviewer-owned requirements-quality* artifacts, so every
item asks what the **artifacts say**, not what the code does — hence **0 PRE-CODE** verdicts. Where
an item's claim will additionally be confirmed on the diff, the task that does so is named.

**This file records an assessment, not a decision.** Nothing here ticks anything; the boxes in
`ux.md`, `reads.md` and `gates.md` are untouched and remain the reviewer's to mark.

## Verdict key

| Verdict | Meaning |
| --- | --- |
| **TRUE-NOW** | The artifacts already satisfy the criterion; evidence cited, tickable today. |
| **PRE-CODE** | Unanswerable until R12's code exists. **None found.** |
| **FALSE** | The artifacts contradict the item, or what it asks for is genuinely missing. |
| **STALE-TEXT** | The item's own wording no longer matches the spec/plan text it describes. |

## Counts

| Checklist | Items | TRUE-NOW | PRE-CODE | FALSE | STALE-TEXT |
| --- | --- | --- | --- | --- | --- |
| `ux.md` | 25 | 12 | 0 | 12 | 1 |
| `reads.md` | 28 | 20 | 0 | 8 | 0 |
| `gates.md` | 25 | 21 | 0 | 4 | 0 |
| **Total** | **78** | **53** | **0** | **24** | **1** |

**Do not tick** the 24 FALSE items or the 1 STALE-TEXT item without first changing the artifact (or,
for the STALE item, the item's own wording).

---

# 1 · `ux.md` — UX & Accessibility (25 items)

| ID | Verdict | Evidence / what is wrong |
| --- | --- | --- |
| CHK001 | **TRUE-NOW** | Order fixed by spec FR-001 (six surfaces, "in that order"); the layout facts that are fixed are fixed in `contracts/screens.md` §2 (component per surface, stat-card skeleton "three card-shaped blocks in a row") and §4 ("Desktop only; no breakpoint"), with vertical stacking implied by FR-038/SC-011's "vertical reflow of content below". Nothing about ordering is left open. |
| CHK002 | **FALSE** | Only the **name form** is pinned — FR-005 ("first name and last name, one space"), research C-6 (`displayName(actor)`, no "Good morning"), `screens.md` §2 row 1, task T004. No artifact fixes the surrounding sentence, so "Ada Lovelace" and "Welcome back, Ada Lovelace" both satisfy every line of text in the feature. **Fix**: state the greeting's literal template in FR-005 or `screens.md` §2. |
| CHK003 | **TRUE-NOW** | The three label texts appear identically in three artifacts: FR-006 ("assigned to you, due this week, unread"), `screens.md` §3 *Stat card*, `quickstart.md` §1; task T009 asserts "a number **and** a text label naming what it counts". |
| CHK004 | **TRUE-NOW** | `screens.md` §2 row 1 gives the greeting "none — it needs only the awaited actor" and the section paragraph puts surfaces 2–6 "in their own `<Suspense>`, **below** the awaited `requireActor()`"; research D-1 forbids `loading.tsx` precisely so nothing streams before the guard; T056 asserts it. So the answer is stated: nothing renders above the boundaries until the actor resolves. |
| CHK005 | **FALSE** | "Desktop only, no breakpoint" appears only in `contracts/screens.md` §4 and `plan.md` *Technical Context* (**Target Platform**). `spec.md` contains no viewport requirement at all — grep for "desktop"/"breakpoint" returns nothing in the spec. **Fix**: add an FR (or an explicit *Assumptions* entry) carrying the viewport expectation. |
| CHK006 | **TRUE-NOW** | Objectively evaluable: FR-006 requires each card to carry a number **and** a text label naming what it counts, so meaning never rests on the card's position; `screens.md` §3 restates it; T009 asserts it with `OT-UX-018`. |
| CHK007 | **TRUE-NOW** | FR-038 quantifies it: reserved rows (5 Mentions, 20 Recent activity, 3 each for the two unbounded sections), "every element MUST keep its horizontal position and its own height", "the frame, heading and per-row geometry its skeleton rendered". SC-011 restates; `screens.md` §2 carries the row counts; T017/T029 assert geometry rather than cardinality. |
| CHK008 | **FALSE** | FR-040 and SC-010 define the empty line only by its negations and its count ("one quiet line", no illustration/marketing/CTA). No artifact gives the four lines' actual text — research D-5 supplies only a **shape** precedent (`NotificationsList`'s "No notifications yet."), and T058/T059 assert "exactly one line", not its wording. **Fix**: specify each section's empty line text, or state that the wording is the implementer's. |
| CHK009 | **FALSE** | FR-042 says "a visible focus indicator" with no criterion; `screens.md` §4 and research E-1 delegate to "the application's shared focus styling" **without naming or bounding it** — the actual rule lives unreferenced in `src/app/globals.css:184-185` (`[data-focus-visible], :focus-visible`). **Fix**: name that shared style (file/selector) in FR-042 or `screens.md` §4 so "visible" is checkable. |
| CHK010 | **TRUE-NOW** | The form is fixed as text: `plan.md` *Technical Context* refuses every charting/sparkline library "because the progress figure is a percentage rendered as text"; `screens.md` §3 lists the row as "name, status, progress percentage"; T029 forbids a `progressbar` in the skeleton. No bar is contemplated, so the bar semantics half is moot. Note that FR-016/FR-019 alone say only "whole-number percentage". |
| CHK011 | **TRUE-NOW** | FR-038's reservations (3 / 3 / 5 / 20) match `contracts/screens.md` §2's *Loading* column exactly: assigned "three rows", projects "three rows", mentions "five rows", activity "twenty rows". |
| CHK012 | **STALE-TEXT** | The item's own wording — "the two unbounded sections the only surfaces permitted to change height" — is not what FR-038 and SC-011 now say. FR-038: "a section holding fewer or more rows than it reserved MAY [change height]", which includes **Mentions** and **Recent activity** whenever they land short, exactly as spec *Edge Cases* ("Fewer than five mentions or fewer than twenty activity rows … without padding") requires. Ticking the item as written would assert something the current spec contradicts. **Fix**: reword the item to "every section whose real row count differs from the count it reserved"; the underlying measurability question is then TRUE-NOW on FR-038 + SC-011. |
| CHK013 | **TRUE-NOW** | Stated per section: `contracts/screens.md` §2's *Empty (FR-040)* column carries "one quiet line" on each of rows 3–6, and "n/a — a zero card renders the digit `0`" on row 2. FR-040 states the one rule; the empty rendering has no row shape for the four to differ in. |
| CHK014 | **TRUE-NOW** | FR-040 final sentence ("A stat card at zero MUST render the number 0 rather than an empty state"), `screens.md` §2 row 2, research D-5, T058. |
| CHK015 | **TRUE-NOW** | Spec *Edge Cases*: "**Fewer than five mentions or fewer than twenty activity rows** — the section renders what exists, without padding and without an empty-row placeholder." |
| CHK016 | **FALSE** | `screens.md` §4 requires only "a labelled region with a heading"; **no heading level is stated anywhere** (grep for `h1`/`h2`/"heading level" across the feature's artifacts hits only `quickstart.md` §6's "no `<h1>` title block"). Accessible names are inferable from FR-001's section names, but the level ladder (greeting vs section headings) is undefined, so "navigable by heading" is unverifiable. **Fix**: fix the levels in `screens.md` §4. |
| CHK017 | **TRUE-NOW** | `screens.md` §4 bullet 1: "an accessible name **from its own text**", and §3 fixes each row's text (issue key + title + project; name + status + percentage; actor + target + relative time). FR-011 and FR-030 fix the same fields. |
| CHK018 | **FALSE** | `aria-busy="true"` appears only in `contracts/screens.md` §4 and in tasks T011/T017/T029/T041/T052. FR-038 does not mention it and neither does any other spec line. **Fix**: carry the obligation into FR-038, or record it as a contract-level detail deliberately outside the spec. |
| CHK019 | **FALSE** | Specified for unread rows only: FR-023 ("an unread row MUST carry the same dot … with a text equivalent"), research E-2 (`sr-only` "Unread" beside an `aria-hidden` dot), T037. A **read** row is only required to "reserve the same box so nothing shifts" — it carries no text, so read-ness is still inferred from the dot's absence. **Fix**: require a text equivalent on both states, or state explicitly that absence is the accepted encoding. |
| CHK020 | **FALSE** | FR-024/FR-030 say only "a relative time". The locale is named (`en-US`, research C-4 and T033) but the **unit ladder is specified only by reference to the shipped implementation** ("its unit ladder", "the full unit ladder … byte-identical in outcome"), and **no artifact says anything about a machine-readable absolute time** (`<time datetime>`) accompanying it. **Fix**: enumerate the ladder (or bind it explicitly to `src/lib/relative-time.ts` as the definition) and state whether a machine-readable timestamp is required. |
| CHK021 | **FALSE** | Only per-row operability is specified — FR-042, `screens.md` §4 bullet 1, T063. No artifact states a traversal order across the six surfaces (grep for "tab order"/"traversal"/"tabindex" hits nothing outside the checklist itself). **Fix**: state that focus order follows FR-001's document order and that no positive `tabindex` is used. |
| CHK022 | **FALSE** | No overflow or truncation rule exists for long issue titles, project names or composed activity sentences. The only truncation words in the artifacts are FR-010's "MUST NOT be truncated" (about **row count**, not text) and research E-3's "invents a truncation rule no source states" (about comment bodies). **Fix**: state a wrap/ellipsis rule, or record that text overflow is the layout's and unconstrained. |
| CHK023 | **FALSE** | Spec *Edge Cases* and `screens.md` §1 say only that the banner "renders above Home's content like it does on every authenticated screen, and nothing on this page is withheld while it does". Neither says anything about heading order or focus order in its presence. **Fix**: state that the banner is R2's and precedes Home's content in DOM, heading and focus order. |
| CHK024 | **TRUE-NOW** | The vocabulary is closed at one value: `data-model.md` §3.2 types the DTO field `status: "active"` ("the query filters to it; the field is rendered"), and FR-014 admits only `status = 'active'`. Matches the schema's `check("project_status_valid", … in ('active','archived'))`. |
| CHK025 | **FALSE** | No requirement carries it. FR-030 enumerates the row's contents but never forbids a body; the decision lives in research E-3, which labels itself "the one interpretive call in the design", plus `data-model.md` §3.4 (DTO has no body) and T048. **Fix**: either add an FR stating the body is not rendered, or record E-3 as adjudicated the way `tasks.md`'s conflicts A–E are. (The checklist's own Notes predict this item fails review.) |

---

# 2 · `reads.md` — Read-correctness (28 items)

| ID | Verdict | Evidence / what is wrong |
| --- | --- | --- |
| CHK001 | **TRUE-NOW** | Resolved to the **database session's** `TimeZone`, not the Node process's: `plan.md` decision 2, research B-1 ("`current_date` is evaluated by PostgreSQL against the database session's `TimeZone`"), `quickstart.md` *Prerequisites* ("Check it with `SHOW TimeZone;`"), T002. Research B-1 explicitly rejects the process-timezone alternative. |
| CHK002 | **TRUE-NOW** | FR-008 fixes both endpoints inclusively ("today through today plus six") plus both exclusions, so an off-by-one is detectable from the requirement text; research F-2 #1 and T007 name all four boundary days (`current_date`, `+6`, `+7`, `-1`) and require asserting the **identity** of the two counted. Note: spec US1 s4 uses `+8` rather than `+7`, so day seven is exercised only via F-2/T007. |
| CHK003 | **TRUE-NOW** | Two separate sentences in FR-008 ("An issue whose `due_date` is already past MUST NOT be counted … An issue carrying no `due_date` MUST NOT be counted"), plus a distinct *Edge Cases* entry covering both. |
| CHK004 | **FALSE** | No artifact addresses a DST transition or an operator changing the installation's timezone mid-day. SC-013 covers only two **browser** timezones; research B-1 argues date-to-date comparison but never states the DST consequence; `quickstart.md` only says to set `TimeZone`. **Fix**: add an *Edge Cases* entry stating that a date-to-date `current_date` comparison is DST-immune and that a timezone change takes effect on the next render. |
| CHK005 | **TRUE-NOW** | Stated positively: FR-014 — "This is the page's **only** project-status filter: **Assigned to you** and **Recent activity** MUST NOT filter on project status" — and the exclusivity clause covers **Mentions** too. `data-model.md` §2's boundary table shows each surface's full scope, with no status predicate outside **Your projects**. |
| CHK006 | **TRUE-NOW** | Both surfaces carry it: FR-009 ("scoped to their own `user_id`") for the unbounded count and FR-027 for the five-row list; `data-model.md` §2 lists "unread card" and "**Mentions**" as separate rows; `contracts/queries.md` §4 gives both `listRecentMentions` and `countUnreadNotifications` their own boundary row; T035 tests both with two seeded users. |
| CHK007 | **TRUE-NOW** | FR-015 states the distinction in terms ("a membership **list**, not the `isMember` predicate — so an admin sees only the projects they were added to explicitly"); research B-4 and `contracts/queries.md` §3 refuse `isMember` by name; T023 asserts it. |
| CHK008 | **TRUE-NOW** | FR-035 is now a signature-level criterion — "This page's route MUST declare no `params`, no `searchParams` and no body" — echoed by `contracts/queries.md` §4 ("the component declares no `params` and no `searchParams`") and encoded in T005; `tasks.md` conflict **A** records the adjudication. The diff-level check is T004/T005. |
| CHK009 | **TRUE-NOW** | `contracts/queries.md` §4 last row: "`limit` — a module constant, never a request value"; T045 asserts "`limit` is a module constant, never a caller- or request-supplied value" and T047 implements "limited to the module constant 20". FR-028 fixes the twenty. |
| CHK010 | **FALSE** | The projection's field set is nowhere stated. `data-model.md` §1 names `publicUser` and lists only what is *excluded* (`email`, `slack_handle`, `phone`, `bio`); the real projection (`src/features/auth/server/projections.ts`) also carries `role`, `jobTitle` and `deactivatedAt`, which no artifact mentions, so FR-037's "no `user` field outside the shared public projection" is verifiable only by opening R1's code. **Fix**: enumerate `publicUser`'s fields in `data-model.md` §1. |
| CHK011 | **FALSE** | The alphabetical key lives in `contracts/queries.md` §5 (`lower(project.name) asc, project.key asc`) and in T023/T024; research B-6 states in terms "Recorded as an **assumption, not a requirement**", and FR-036 obliges **Your projects** only to "order deterministically". No requirement carries the key. **Fix**: name the key in FR-036, or record the contract as the binding source the way `tasks.md` conflict **B** does for the tiebreak. (The checklist's own Notes predict this item fails review.) |
| CHK012 | **TRUE-NOW** | Stated across tables, not only within one: `data-model.md` §4 orders the union itself (`order by created_at desc, id desc` applied to `comment ∪ activity`), `contracts/queries.md` §5 says "`created_at desc, id desc` **across the union**", and T045 requires "ties across the union's **two source tables** resolve identically across two reads". |
| CHK013 | **TRUE-NOW** | Bound to issue **creation**: spec *Assumptions* ("Rows are ordered by their creation instant … every table involved carries `created_at`") and `contracts/queries.md` §5 (`issue.created_at desc, issue.id desc`); T006 asserts it. Assignment time and last-update are excluded by naming `created_at`. |
| CHK014 | **TRUE-NOW** | FR-031: "Twenty rows MUST mean twenty rows, so the count is a property of the data rather than of what a given reader's collapsing produced", with FR-028's "twenty most recent rows"; T045 tests five changes in five minutes producing five rows. |
| CHK015 | **TRUE-NOW** | The two values that matter are literal: research B-3 and `data-model.md` §4 give `count(*) filter (where board_column.kind = 'done')` and `count(*) filter (where board_column.kind <> 'canceled')`; FR-017 states the formula and FR-020 the read-only boundary; T024 implements exactly those two aggregates. |
| CHK016 | **FALSE** | Half specified. A column whose kind is neither `done` nor `canceled` is covered (FR-017's "total issue count minus the count sitting in a `canceled`-kind column"; `kind <> 'canceled'` counts it). An issue **in no column** is addressed nowhere, and the `data-model.md` §4 shape would silently drop it (`NULL <> 'canceled'` is NULL, so the `filter` skips it) while FR-017's text would count it. The case is in fact unrepresentable — `src/db/schema.ts:263` declares `columnId … .notNull()` — but no artifact says so. **Fix**: one sentence in `data-model.md` §3.2/§4 recording that `issue.column_id` is `NOT NULL`. |
| CHK017 | **TRUE-NOW** | All five with expected values in `data-model.md` §3.2's table (3/8→38, 0/0→0, 199/200→99, 200/200→100, 0/5→0); research F-2 #2 and T021 require them in **one** Red step precisely so a clamp-everything implementation cannot pass. |
| CHK018 | **TRUE-NOW** | The endpoints are separately asserted: FR-019's two clauses, two distinct *Edge Cases* entries (all-done reads 100%; "no rounding rule may turn that into 99%"), US2 s6 for the 99% side, and two separate rows of `data-model.md` §3.2's table. |
| CHK019 | **TRUE-NOW** | FR-018 forbids each alternative by name ("It MUST NOT read blank, a dash, \"n/a\", or raise an error"); SC-004 repeats it; T025 asserts no data shape produces one. |
| CHK020 | **TRUE-NOW** | FR-029 settles which row renders (the log's `comment`-type record "MUST NOT render as a second row", so the `comment` row is the survivor); research B-5 gives the reason and `data-model.md` §3.4 types `kind: "comment"` for rows drawn from the `comment` table; the link target is the `#comment-<id>` anchor on the target (research E-3, `screens.md` §3). |
| CHK021 | **TRUE-NOW** | Every type is eligible and only the duplicate is dropped: research B-5's union excludes `activity.type = 'comment'` alone; `data-model.md` §3.4 types `kind` as "`ActivityType` minus `\"comment\"`"; research C-5 covers "the ten non-comment activity types" against `plan.md`'s "`writeActivity`'s eleven types". |
| CHK022 | **FALSE** | The deleted-comment-behind-a-mention case has its own *Edge Cases* entry; the equivalent for an activity or comment row whose target **issue or project** was deleted has none, and neither `data-model.md` §4 nor `contracts/queries.md` says what the union or the target hydration does with a missing target. **Fix**: add an *Edge Cases* entry (R5/R6/R7's cascades presumably remove the rows — state it). |
| CHK023 | **FALSE** | Only the **deactivated** actor is covered (spec *Edge Cases*, twice). An absent actor row is addressed nowhere, while `data-model.md` §4's `listRecentMentions` shape uses an **inner** join to the actor `user`, which would silently drop the row. **Fix**: state that user rows are never deleted (deactivation only), or define the render behaviour. |
| CHK024 | **FALSE** | Not required. FR-005 binds only the greeting to the display-name rule; FR-024 and FR-030 say merely "name the actor", and `contracts/queries.md` §2 lists `displayName` as used for "the greeting" alone. Mention rows inherit R11's `actorName` composition through FR-024's "matching the Notifications screen's own row", but nothing binds **Recent activity**'s actor names to the same rule. **Fix**: state in FR-030 (or `data-model.md` §3.4) that every rendered actor name uses `displayName`. |
| CHK025 | **TRUE-NOW** | Checkable as an import census, not an intent: research C-2 ("the absence of the import **is** the requirement"), `contracts/queries.md` §3 refuses `MENTION_TOKEN_PATTERN`, `mention-resolve.ts` and `mention-queries.ts` by name, and T061 asserts no module under `src/features/home/` imports any of them; FR-022 states the prohibition. |
| CHK026 | **TRUE-NOW** | The binding obligation is at spec level: FR-007 ("MUST count exactly the issues **Assigned to you** lists under FR-010, so the card and the section beneath it can never disagree") and SC-002 ("for every user and every data shape"). The single cached read is the mechanism, recorded in research B-2 and `contracts/queries.md` §1 (`cache()`), and T019 tests the equality. |
| CHK027 | **TRUE-NOW** | The same count, not two agreeing values: FR-009 ties the card to the sidebar's unbounded count; research D-2 makes it "two call sites of one query rather than two implementations of one count"; `contracts/queries.md` §2 lists `countUnreadNotifications` as called unchanged; T009 asserts equality for a viewer holding more than 200 rows. |
| CHK028 | **FALSE** | No bound, response-size or latency expectation is stated anywhere. FR-010 states the deliberate absence of a row bound; `plan.md` *Performance Goals* says "none stated by the specification and none invented"; *Complexity Tracking* row 4 accepts the two unindexed patterns "with its cost stated" (§7's single box, under twenty people). The absence is deliberate and recorded — but the item asks whether an expectation **is stated**, and none is. **Fix**: either state a figure, or record the non-requirement explicitly in the spec so the item can be closed against it. |

---

# 3 · `gates.md` — Change gates (25 items)

| ID | Verdict | Evidence / what is wrong |
| --- | --- | --- |
| CHK001 | **FALSE** | 68 tasks and 27 scenarios confirmed, and 19 scenarios are cited by ID in `tasks.md`; of the remaining eight, seven are reachable through their FR/SC citations (US1 s1→T004+T009, s2→T006+T019, s3→T009; US3 s1/s2→T035+T037, s4→T060; US5 s1→T060). **US5 s2 has no Red step**: FR-039's "re-query on revisit, nothing from a client cache" is asserted only by T061's negative census (no `"use cache"`, `unstable_cache`, `revalidatePath`, `refresh()`) and walked manually in `quickstart.md` §16 via T068 — neither asserts the scenario. **Fix**: add a Red task for FR-039, or record that research D-3 makes it a framework property whose proof is the census. (Gate 1's commit-order evidence is necessarily future; that half is settled by the commit history and T067.) |
| CHK002 | **TRUE-NOW** | Criteria are given for both groups: research F-2 names the four subtle Red steps and says exactly what a wrongly-written version would pass against; `tasks.md` Phase 7's note requires T060, T061 and T062 to be observed failing "against a deliberately broken local variant … which is then reverted"; for the rest the implementation file does not yet exist, and `tasks.md`'s header binds "run and seen to fail" per pair. |
| CHK003 | **TRUE-NOW** | `plan.md` *Gates 1–8* row 2: "Scoped per task. Each query does no more than its row in `contracts/queries.md` §1", and §1 gives each function its module, signature and requirement list. |
| CHK004 | **TRUE-NOW** | `quickstart.md` *Prerequisites* requires two databases and names the test one by its variable (`TEST_DATABASE_URL`, "Never point either at staging or production"); research F-1 names all four queries as tested against the real database `src/db/test-setup.ts` migrates; T002 verifies the separation before any query task starts. |
| CHK005 | **TRUE-NOW** | Stated as a split: research F-1 ("`progressPercent` is tested in the same project **with no database at all**"), `plan.md` *Testing* ("The rounding rule is tested with no database at all"), T021 ("**no database**") and T022 ("No database import"). |
| CHK006 | **TRUE-NOW** | FR-035's "declare no `params`, no `searchParams` and no body" is what gate 3 is satisfied against; `plan.md`'s Principle II row and research D-4 explain why no validator is written, and `tasks.md`'s conflict **A** records the adjudication with its tasks (T004, T005, T019). |
| CHK007 | **TRUE-NOW** | `contracts/queries.md` §4 covers every **entry point** the feature adds — the route plus all five called query functions — each with accepted input, authentication and authorization. The two non-boundary additions (`progressPercent`, `formatRelativeTime`) take no external input and are specified in §1 and in `plan.md` *Project Structure* instead. |
| CHK008 | **TRUE-NOW** | Testable, not a signature claim: research F-2 #4 requires **two** seeded users for both `listRecentMentions` and the unread count; T035 encodes it; SC-007 says "verifiable by attempting it as a second user" and US3 s5 is the scenario. |
| CHK009 | **TRUE-NOW** | `plan.md` gate 4 row ("None installed; `package.json` is unchanged") plus the closing *Complexity Tracking* paragraph, which restates `clsx@^2.1.1` (confirmed at `package.json:26`, absent from `AGENTS.md`'s table) as inherited from before R11 and explicitly not this feature's to resolve; T003 records the config baseline. |
| CHK010 | **TRUE-NOW** | `plan.md` *Technical Context*, "Dependencies this feature deliberately refuses": `date-fns`, `dayjs`, `luxon`, `moment-timezone`, every charting/sparkline library, `swr`, `@tanstack/react-query`, `@react-aria/test-utils` — each with its reason. |
| CHK011 | **TRUE-NOW** | Reconciled explicitly: `plan.md` *Technical Context* ("**`react-aria-components` is not imported by this feature at all**, and that is not an oversight"), research E-1 (`OT-UX-018` requires React Aria where behaviour must be *reproduced*), `screens.md` §4, research F-4 for `@react-aria/test-utils`. |
| CHK012 | **TRUE-NOW** | `plan.md` Principle V row: "No comments in the diff. The five places a reader will want an explanation … are answered by `research.md` B-1, B-2, B-5, C-3 and E-1 and by the contracts, **not by annotation**"; T066 audits the diff for comments and commented-out code. |
| CHK013 | **TRUE-NOW** | Recorded as a VI decision, not an omission: `plan.md` Principle VI row ("The largest single application is the **absence of a validator** for input the route cannot receive (D-4)"), research D-4, `tasks.md` conflict **A**, and T004's instruction to assert that no validator exists. |
| CHK014 | **TRUE-NOW** | All three omissions are tied to VI in `plan.md`'s Principle VI row and restated at their DTOs: `data-model.md` §3.1 (no `dueDate`), §3.2 (no `percent` — "The percentage is **not** a field"), §3.4 with research E-3 (no comment body). |
| CHK015 | **FALSE** | FR-044's additive carve-out covers exactly **one** of the four edits (the `listRecentMentions` export; T036 requires the two existing functions byte-identical). The other three are not additive and rest on different grounds: the import swaps in R7's `comment-row.tsx` and R11's `notification-row.tsx` are permitted as behaviour-preserving **moves** approved by the user (`plan.md` *Complexity Tracking* row 2, research C-4, `tasks.md` conflict **D**, T034), and `page.test.ts`'s assertion is a named carve-out. So "every such edit is checkably additive" cannot be ticked as written. **Fix**: extend FR-044 to say a behaviour-preserving move with untouched test files is likewise not an alteration, so all four edits trace to spec text rather than to a plan-level approval. (Diff-level confirmation: T034, T036, T065, T066.) |
| CHK016 | **TRUE-NOW** | Stated: `plan.md` *Complexity Tracking* row 2 — "**This edit is approved by the user** … the task carrying it must show both components' existing suites green and unmodified before it is accepted" — encoded in T034 (both named suites green with those files unmodified in the diff) and in `tasks.md` conflict **D**. |
| CHK017 | **FALSE** | SC-014's text speaks only about **test** files ("Every existing test in entries R1 through R11 passes unmodified … No other R1 through R11 test moves"). It never says whether editing an R7 or an R11 **source** file is permitted; that permission comes from FR-044's "no alteration of … behaviour" plus `tasks.md` conflict **D**'s adjudication. **Fix**: one clause in SC-014 (or FR-044) stating that behaviour-preserving source edits with untouched test files are permitted. |
| CHK018 | **FALSE** | Three of the four are tied to requirements in `plan.md` *Project Structure* (`notification-queries.ts` → FR-021/FR-022/FR-024/FR-025/FR-027; `page.test.ts` → FR-001/FR-002/FR-044/SC-014; `lib/relative-time.ts` → FR-024/FR-030). The two component edits are annotated **`C-4`** — a research decision and Principle I, not a requirement — and `plan.md` itself concedes the point: "it is the only change in this feature to a file another entry owns **for a reason other than a requirement naming that file**". **Fix**: same one-line fix as CHK015, or state Principle I plus the recorded approval as the accepted gate-7 trace. |
| CHK019 | **TRUE-NOW** | `plan.md` *Project Structure*: "**One edit to `docs/ROADMAP.md`** … R12's `Sub-spec` cell points at `specs/011-home-roll-up/`. `Status` stays `planned`; **no version is bumped and no history or status-log row is added**"; T064 verifies rather than repeats it. The working tree's diff is exactly that one cell. |
| CHK020 | **TRUE-NOW** | FR-020 states the boundary in terms ("MUST read the `done` and `canceled` column kinds and MUST NOT enforce, restate or re-implement invariant 14"), spec *Out of Scope* repeats it ("Reading the kinds is not enforcing them"), and T061's census asserts that no module enforces, restates or re-implements it. |
| CHK021 | **TRUE-NOW** | Recorded with an owner and no task acting on it: `plan.md` *Complexity Tracking*, "Not recorded as a violation … **The finding is real and belongs to R7's owners**"; research B-5; `plan.md` *Explicitly out of scope* ("fixing R7's duplicate comment row in `listFeed`"); `tasks.md`'s scope reminders keep `feed-queries.ts` untouched. |
| CHK022 | **TRUE-NOW** | `quickstart.md` *The gate*: "**A green `npm test` is not evidence of test-first** — it runs with `--passWithNoTests`. The commit order is the evidence (gate 1)"; `plan.md` gate 8 row says the same and adds SC-014's no-test-file-moved requirement; T001 records the baseline test count so no R1–R11 test can be lost or skipped, and T065 audits it. |
| CHK023 | **TRUE-NOW** | Research F-3: "a full row census of **every table** in `src/db/tables.ts`' `ALL_TABLES` … not as a check that a particular table is unchanged"; T060 restates it as "over **every** table … not a spot check". |
| CHK024 | **TRUE-NOW** | `quickstart.md` *What a browser cannot show*, last row: "a file census: no `\"use client\"` under `src/features/home/`"; T061 implements it as a static census (also covering `"use server"` and `actions.ts`); `plan.md` decision 1 and research A-1 make it structural. |
| CHK025 | **TRUE-NOW** | `plan.md` Principle I row documents both directions: the one extraction is at its "**already-confirmed second** call site" (research C-4 — and the tree confirms exactly two call sites, `comment-row.tsx` and `notification-row.tsx`), and the declined abstractions are named (no shared roll-up section wrapper, no shared stat-card abstraction, `src/components/ui/` still not created). |

---

## Cross-cutting observations

- **The FALSE items cluster in three places.** (a) UX/a11y detail that the artifacts delegate without bounding — CHK008, CHK009, CHK016, CHK018, CHK019, CHK020, CHK021, CHK022, CHK023 in `ux.md`; each is a one- or two-sentence addition to FR-042/FR-040 or `contracts/screens.md` §4. (b) Edge cases nobody wrote down because the schema or a cascade already forbids them — `reads.md` CHK004, CHK016, CHK022, CHK023. (c) The reach-back cluster — `gates.md` CHK015, CHK017, CHK018 all resolve with **one** amendment to FR-044/SC-014 saying a behaviour-preserving move with untouched tests is not an alteration.
- **Two items the checklists themselves predicted would fail do fail**: `ux.md` CHK025 (comment body rests on research E-3) and `reads.md` CHK011 (the alphabetical key lives in the contract alone).
- **One item is stale rather than wrong**: `ux.md` CHK012 describes a version of FR-038 that no longer exists.
- **Nothing is blocked on code.** Where a claim will also be checked against the diff, the settling task is named above (most often T034, T036, T060, T061, T065, T066, T067).
